name: Go

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  generate:
    name: Generate
    runs-on: ubuntu-latest
    container: grpc/go

    steps:
    - name: Set up Go 1.13
      uses: actions/setup-go@v1
      with:
        go-version: 1.13
      id: set-go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Go to directory
      run: |
        cd proto

#    - name: GRPC
#      run: |
#        go get -u google.golang.org/grpc

#    - name: Proto-gen-go
#      run: |
#        go get -u github.com/golang/protobuf/protoc-gen-go

#    - name: Set path
#      run: |
#        echo ${GOPATH}; export PATH=$PATH:$GOPATH/bin

#    - name: Get dependencies
#      run: |
#        go test

#    - name: Get dependencies
#      run: |
#        go get github.com/golang/protobuf@master

    - name: protoc-gen-micro
      run: |
        GO111MODULE=on  go get github.com/micro/micro/v2@v2.4.0

    - name: Set path
      run: |
        export PATH=$PATH:$GOPATH/bin

    - name: Generate code
      run: |
        protoc --proto_path=$GOPATH:. --micro_out=. --go_out=. Devbench.proto


  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

    - name: Set up Go 1.13
      uses: actions/setup-go@v1
      with:
        go-version: 1.13
      id: go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v2

    - name: Get dependencies
      run: |
        go get -v -t -d ./...
        if [ -f Gopkg.toml ]; then
            curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
            dep ensure
        fi

    - name: Build
      run: go build -v .
